
import {
  FETCH_{{constantCase entityName}}_SUCCESS,
  FETCH_{{constantCase entityName}}_FAILURE,

  UPDATE_{{constantCase entityName}}_SUCCESS,
  UPDATE_{{constantCase entityName}}_FAILURE,

  DELETE_{{constantCase entityName}}_SUCCESS,
  DELETE_{{constantCase entityName}}_FAILURE,

  CREATE_{{constantCase entityName}}_SUCCESS,
  CREATE_{{constantCase entityName}}_FAILURE
} from "../actions/{{camelCase entityName}}ActionCreator";

const initialState = {
  
};

const {{camelCase entityName}}Reducer = (state = initialState, action) => {
  switch (action.type) {
    case FETCH_{{constantCase entityName}}_SUCCESS: {
      const { payload } = action;
      return {
        ...state,
        payload,
      };
    }
    case FETCH_{{constantCase entityName}}_FAILURE: {
      const { error } = action;
      return {
        ...state,
        error,
      };
    }
    case CREATE_{{constantCase entityName}}_SUCCESS: {
      const { payload } = action;
      return {
        ...state,
        payload,
      };
    }
    case CREATE_{{constantCase entityName}}_FAILURE: {
      const { error } = action;
      return {
        ...state,
        error,
      };
    }
    case UPDATE_{{constantCase entityName}}_SUCCESS: {
      const { payload } = action;
      return {
        ...state,
        payload,
      };
    }
    case UPDATE_{{constantCase entityName}}_FAILURE: {
      const { error } = action;
      return {
        ...state,
        error,
      };
    }
    case DELETE_{{constantCase entityName}}_SUCCESS: {
      const { payload } = action;
      return {
        ...state,
        payload,
      };
    }
    case DELETE_{{constantCase entityName}}_FAILURE: {
      const { error } = action;
      return {
        ...state,
        error,
      };
    }

    default:
      return state;
  }
};

export default {{camelCase entityName}}Reducer;
