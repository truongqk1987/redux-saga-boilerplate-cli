
import {
  FETCH_{{constantCase entity}}_SUCCESS,
  FETCH_{{constantCase entity}}_FAILURE,

  UPDATE_{{constantCase entity}}_SUCCESS,
  UPDATE_{{constantCase entity}}_FAILURE,

  DELETE_{{constantCase entity}}_SUCCESS,
  DELETE_{{constantCase entity}}_FAILURE,

  CREATE_{{constantCase entity}}_SUCCESS,
  CREATE_{{constantCase entity}}_FAILURE
} from "../actions/{{camelCase entity}}ActionCreator";

const initialState = {
  
};

const {{camelCase entity}}Reducer = (state = initialState, action) => {
  switch (action.type) {
    case FETCH_{{constantCase entity}}_SUCCESS: {
      const { payload } = action;
      return {
        ...state,
        payload,
      };
    }
    case FETCH_{{constantCase entity}}_FAILURE: {
      const { error } = action;
      return {
        ...state,
        error,
      };
    }
    case CREATE_{{constantCase entity}}_SUCCESS: {
      const { payload } = action;
      return {
        ...state,
        payload,
      };
    }
    case CREATE_{{constantCase entity}}_FAILURE: {
      const { error } = action;
      return {
        ...state,
        error,
      };
    }
    case UPDATE_{{constantCase entity}}_SUCCESS: {
      const { payload } = action;
      return {
        ...state,
        payload,
      };
    }
    case UPDATE_{{constantCase entity}}_FAILURE: {
      const { error } = action;
      return {
        ...state,
        error,
      };
    }
    case DELETE_{{constantCase entity}}_SUCCESS: {
      const { payload } = action;
      return {
        ...state,
        payload,
      };
    }
    case DELETE_{{constantCase entity}}_FAILURE: {
      const { error } = action;
      return {
        ...state,
        error,
      };
    }

    default:
      return state;
  }
};

export default {{camelCase entity}}Reducer;
