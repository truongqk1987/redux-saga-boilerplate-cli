
import {
  FETCH_{{constantCase entityNam}}_SUCCESS,
  FETCH_{{constantCase entityNam}}_FAILURE,

  UPDATE_{{constantCase entityNam}}_SUCCESS,
  UPDATE_{{constantCase entityNam}}_FAILURE,

  DELETE_{{constantCase entityNam}}_SUCCESS,
  DELETE_{{constantCase entityNam}}_FAILURE,

  CREATE_{{constantCase entityNam}}_SUCCESS,
  CREATE_{{constantCase entityNam}}_FAILURE
} from "../actions/{{camelCase entityName}}ActionCreator";

const initialState = {
  
};

const {{camelCase entityName}}Reducer = (state = initialState, action) => {
  switch (action.type) {
    case FETCH_{{constantCase entityNam}}_SUCCESS: {
      const { payload } = action;
      return {
        ...state,
        payload,
      };
    }
    case FETCH_{{constantCase entityNam}}_FAILURE: {
      const { error } = action;
      return {
        ...state,
        error,
      };
    }
    case CREATE_{{constantCase entityNam}}_SUCCESS: {
      const { payload } = action;
      return {
        ...state,
        payload,
      };
    }
    case CREATE_{{constantCase entityNam}}_FAILURE: {
      const { error } = action;
      return {
        ...state,
        error,
      };
    }
    case UPDATE_{{constantCase entityNam}}_SUCCESS: {
      const { payload } = action;
      return {
        ...state,
        payload,
      };
    }
    case UPDATE_{{constantCase entityNam}}_FAILURE: {
      const { error } = action;
      return {
        ...state,
        error,
      };
    }
    case DELETE_{{constantCase entityNam}}_SUCCESS: {
      const { payload } = action;
      return {
        ...state,
        payload,
      };
    }
    case DELETE_{{constantCase entityNam}}_FAILURE: {
      const { error } = action;
      return {
        ...state,
        error,
      };
    }

    default:
      return state;
  }
};

export default {{camelCase entityName}}Reducer;
